{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口默认能力（无跨进程/文件系统权限——演控台只经 localhost WS 与 NT4 通信）",
  "windows": ["main"],
  "permissions": ["core:default"]
}
