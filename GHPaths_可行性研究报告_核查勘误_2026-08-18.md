# GHPaths 可行性研究报告 — 独立核查与勘误

> 核查日期:2026-08-18(与报告 v1.1 同日)。**报告已于同日修订为 v1.2,应用了本勘误的全部 A 类与 B 类修正。**
> 方法:11 组并行核查代理,对报告的 41 条承重论断逐条核验,全部对一手来源(GitHub 仓库源码/LICENSE/Release、docs.wpilib.org 各年份版本对比、frc-radio.vivid-hosting.net、Apple TN3179、WPILib 博客原文、Chief Delphi 维护者发言)。
>
> **总体结论:报告的架构判断(一网多机组网、multi-DS 攻坚方向、NT4 监控、Mac 主力、安全体系、分阶段路线)全部成立,无需改动。** 但有 2 处硬伤、2 处关键过时需要修正,若干细节建议修订;其中 3 处修正对项目实际上是**利好**。

---

## A. 必须修正的(影响决策)

### A1. "2025 年起 roboRIO 烧录跨平台化"不成立 【§7.1 "roboRIO 烧录/镜像" 行】

- 实况(截至 2026-08):**roboRIO 1 的烧录仍然必须用 NI FRC Game Tools(仅 Windows)**。2026 赛季 stable 文档原文仍要求"烧录前必须完成 FRC Game Tools 安装";WPILib 维护者在 Chief Delphi 明确表示镜像文件没有 Game Tools 之外的合法获取渠道;枚举 wpilibsuite 组织全部 133 个仓库,不存在任何跨平台烧录工具(唯一跨平台的 Java 工具是 Team Number Setter,它不改镜像)。
- roboRIO 2.0 的 microSD 写卡流程真实存在(但 2022 年起即有,非 2025 新增),写卡工具(balenaEtcher / Raspberry Pi Imager)跨平台——但镜像文件的获取仍经由 Windows 上的 Game Tools 安装目录。
- 推测报告混淆源:Team Number Setter(跨平台 Java,但不烧录)、2025 Beta 期间临时发布在 GitHub 的测试镜像、或 2027 Systemcore 的未来计划。
- **影响**:§7.2"需要 Windows 的清单"增加第 3 项——roboRIO 初始烧录/重刷镜像。若 6 台机器人到手时已预装镜像(常见),日常开发排练无感,仅在需要重刷时借道 Windows。

### A2. "官方 JS/TS NT4 客户端"不存在 【§六.2 决策理由 (c)、参考资料】

- wpilibsuite/NetworkTablesClients 实际**只包含一个 .NET(C#)NT3 客户端**:全部 4 次提交停在 2022-07,近乎休眠。WPILib 官方 NT 客户端只有 C++/Java(allwpilib ntcore)与 Python(RobotPy),**没有任何官方 JS/TS 实现**;npm 上全是社区包。
- 报告描述的能力(JS/TS、NT4 over WebSocket、Node + 浏览器)实际对应的是社区项目 **cjlawson02/ntcore-ts**——而且它状态很好:2026-08-18 当天仍有提交(在跟进 WPILib 2027),明确支持一个进程连多个服务器(正是 6 机聚合的用法)、Node + 浏览器双端、struct topics 内置 Pose2d 等 WPILib 几何类型、配套 `@ntcore-ts/react` hooks。注意当前一代 API 是 `@ntcore-ts/client` 1.0.0-beta.1(旧稳定线 `ntcore-ts-client` 3.1.3 停在 2025-04)。
- **影响**:架构不变,引用要改;风险点 = 单人维护 + beta。建议:NT 接入层自己包一层薄适配接口(随时可换实现/可 fork);NT4 协议有官方规范(WebSocket + JSON 控制帧 + MessagePack 数据帧,端口 5810 明文 / 5811 TLS),自研一个"订阅 pose + 发布命令"的薄客户端也在一天工作量级,可作为兜底。

### A3. 无线电章节描述的是 OpenMesh 时代流程,当前无线电已换代 【§3.2、§7.1、§7.2】

- "FRC Radio Configuration Utility 的 FMS Offseason 模式 → 桥接加入自选 SSID"是 **2017-2024 OpenMesh OM5P 时代**的流程。该 Windows 工具现为遗留工具(2026 赛季仅台湾/中国大陆赛区仍在用 OpenMesh);对应的 WPILib 文档页已下线(stable 版重定向、2026 版 404);frc-radio.vivid-hosting.net 上也没有"FMS Offseason 模式",取而代之的是 "Offseason Kiosk Programmer"。
- 当前官方无线电 **Vivid-Hosting VH-109**(2025+,Wi-Fi 6E)完全是另一套:**配置走任意操作系统的网页**(radio.local / 192.168.69.1);offseason 场景用**跨平台 Docker** 的 Kiosk Programmer + 每队 WPA 密钥 CSV;真正的多机场地 AP 是 **VH-113**(VH-109 可刷 VH-113 固件改造,需管理型交换机 + VLAN)。VH-109 普通机器人模式的 SSID 由队号派生(FRC-XXYY),能否在网页里直接指定加入任意自选 SSID 未在官方文档确认。
- **影响**:
  - (a) §7.2 的 Windows 清单里**无线电刷写一项可能可以划掉**——若机器人装的是 VH-109,刷写全程 Mac 可完成(网页 + Docker),这是利好;
  - (b) 若是 OM5P,报告原流程成立且确实需要 Windows;
  - (c) 组网细节要按实际无线电型号重新定(自备企业级路由器 + VH-109 客户端加入的可行性需 Phase 0 实测)。
  - → **Phase 0 前置盘点:6 台机器人各用什么无线电、roboRIO 当前镜像版本。这两个硬件事实直接决定组网方案与 Windows 依赖程度。**

### A4. 2027 换代时间线比报告更激进,但对表演项目反而有利 【§5.4】

- 新 DS 已在 alpha(wpilibsuite/FirstDriverStation-Public,2026-03 起活跃),跨平台含 macOS arm64——但**只支持 Systemcore,完全不兼容 roboRIO**。roboRIO 车队不是"不被迫迁移",而是根本用不上新 DS,继续用现有 NI DS(官方口径:2026 版之后预计不再有更新)。
- 更关键:WPILib 已于 **2026-05-02 停止 roboRIO 侧的维护**(2027 alpha 不兼容 roboRIO);FIRST 官宣 2026-27 赛季起启用 Systemcore;roboRIO 的比赛合法性到 2026 赛季为止(社区信源一致;2027 手册尚未发布,此点待官方最终确认)。
- **对本项目的含义(利好)**:表演不上场比赛。**锁定 2026 冻结镜像 + 2026 WPILib 栈 = DS 协议与机器人软件栈从此不再逐年漂移**。§九"DS 协议逐年微调"的中风险实际大幅下降——multi-DS 一次做对,可以稳定服役很多年;"每赛季初回归测试"可降级为"仅升级镜像时测试"。只有未来采购 Systemcore 新机器人时才需要重新面对(注意:新 DS 为闭源)。

---

## B. 建议修订的(精度)

| # | 位置 | 修订内容 |
|---|------|---------|
| B1 | §5.3 | **Conductor 已归档**(2026-04-24,与新 DS 公告同日;作者明言"任何场景都不再推荐使用")。**ds-rs crate 是宽松双许可 MIT/Apache-2.0**——与 OpenDS 相反,**其代码可以合法复用进本项目**;但其协议数据自述只对 2018-2020 机器人校验过,且 2020 年后无实质维护。定位:可复用的协议骨架,需对 2026 实测校验。(注意 GitHub API 只显示 Apache-2.0,实为双许可,以 Cargo.toml 为准) |
| B2 | §7.1 | QDriverStation 休眠(最后 release 2021-04,最后一次提交 2025-11 的社区 Qt6 移植),其 README 自述**不支持 2020+ 机器人**;只配当 LibDS 协议学习参考,应从"调试参考用"里删掉 |
| B3 | §7.1 | Choreo 桌面工具是 **TypeScript/Rust/C++(Tauri 应用)**,不是 Java;Java/C++/Python 的是机器人侧 ChoreoLib。Choreo 唯一的"他者"概念是静态障碍物避让(beta),与多机协同无关 |
| B4 | §5.3 | OpenDS 协议覆盖为 **2014-2016 + 2020-2026**(2017-2019 无实现,与本项目无关);最新 v0.3.1(2026-03-04),最后提交 2026-03-11,维护状态与报告一致。许可证四条款核实无误,"只读学习、净室实现"的红线结论不变 |
| B5 | §5.3 | DS 端口 1740/1110/1150 全部确认,但**引用来源要换**:FIRST 官方 FMS 白皮书/游戏手册的端口表是过时的(写着 UDP 1130/1140,且漏了 1740)——请引用 FRCture(frcture.readthedocs.io,逆向工程协议规范)或 LibDS 源码。另有细节:FMS 场内控制包走 UDP **1115**(非 1110);offseason FMS 用 1120(需操作员确认)、正式赛 1121;1735 是 NT3 时代 SmartDashboard 端口,NT4 仪表盘不用它。看门狗确认由 NI NetComm 层执行(用户代码无法绕过),失联数百毫秒内切断输出 |
| B6 | §4.2 | NT4 服务端口 5810(明文)/ 5811(TLS);默认周期性批量合并更新,最低延迟需调小 update interval 或显式 flush();多实例上限 16(含默认实例),文档页现名 "NetworkTables Instances"。6 机 × 20-50ms pose ≈ 每秒一两百条小消息、几 KB/s,负载判断成立 |
| B7 | §六.5、§7.3 | macOS 本地网络权限(TN3179)精确语义:只管**出站连接**与组播/广播接收;**监听/接受入站 TCP、接收单播 UDP 不受限**(机器人→DS 的 UDP 1150 上行不受影响;受影响的是 NT 客户端出站与 mDNS 发现)。**从 Terminal/SSH 启动的命令行工具及其子进程豁免**——排障/排练期的实用 workaround。macOS 15.5+ 可用 `sudo defaults` 写 `AllowedEthernetLocalNetworkAddresses` 白名单整个子网,对固定机器人网段是干净解法。防火墙弹窗只在防火墙开启时出现(系统默认关闭),签名 app 自动放行 |
| B8 | §六.5 | ifconfig alias 确认可行。同子网 alias 建议 /32 掩码避免路由遮蔽;本项目 6 个队子网互不相同,用 /24 反而正确(各自提供路由)。alias 会在网卡 bounce(WiFi 重连/DHCP 续租)时丢失——视为会话级临时配置 + GHPaths 启动自检(报告已有此设计,补 bounce 诱因) |

---

## C. 核查全部确认的关键项(不展开)

- 组网架构判断(一网多机 ✅ / 6 网卡直连不可行 ✅)、DS 20ms 心跳 + 看门狗使能机制、官方 DS 单实例(技术性约束:固定端口 + 单队号模型,非官方政策条文)
- 多 NetworkTableInstance 官方支持(上限 16)
- PathPlanner / Choreo **均无多机支持**(Choreo 项目格式源码级确认:单 RobotConfig;另:Team 103 的 PathPlanner 协作 fork 是社区仅有的多机可视化先例,可作交互参考)
- PathPlannerLib MIT、macOS dmg(v2026.1.2,2026-01-12)、可加载 Choreo 轨迹(预生成,不支持运行时重规划);ChoreoLib BSD-3、全平台分发(v2026.0.3)
- AdvantageScope macOS/Apple Silicon 构建(v26.0.2)+ 3D Field + live NT4
- CheesyArena Lite **活跃维护**(v2.0.0,2026-06-14,Go 1.26,含 macOS M1 构建);许可确认练习/offseason 免费用(改版再分发需 Team 254 许可——自用无碍)。彩蛋:`-dev` 启动标志把 DS 监听绑到任意 IP,**无机器人也能开发调试**,对我们的 PoC 有直接参考价值
- roboRIO 2.0 microSD 烧录流程(2022 年起)
- 2027 DS 博客(2026-04-24,标题/日期/作者/内容全对)
- "演控机 10.TE.AM.5" 惯例(WPILib IP Configurations 文档确认);FMS-offseason 模式下 roboRIO 由机器人侧无线电 DHCP 保留地址获得 10.TE.AM.2(依赖主机名匹配),mDNS 名 roboRIO-XXXX-FRC.local 是稳健寻址方式

---

## D. 对开发的直接影响(结论)

1. **Phase 0 前置盘点**(比报告原定的"单机链路验证"更早一步):6 台机器人的无线电型号(OM5P 还是 VH-109)与 roboRIO 镜像版本。这决定组网方案、Mac 依赖面、以及是否还需要 Windows。
2. **协议资料优先级**:FRCture 规范文档(干净、系统) → ds-rs(宽松许可,代码可复用,但 2018-2020 数据需对 2026 实测校验) → OpenDS / LibDS(只读学习,红线)。最终以 2026 实机抓包为准。
3. **NT 接入层**:自己包一层薄适配接口;内部先用 `@ntcore-ts/client`(beta,活跃)或自研薄客户端,两条路都通。
4. **锁定 2026 冻结栈**:镜像、WPILib、vendordep 全部钉在 2026 版;协议回归测试降级为"升级时才做"。
5. **总体架构(§六)与技术栈选型无需改动**;§7.2 Windows 清单按 A1/A3 修订(可能是"roboRIO 烧录"进、"无线电刷写"出)。

---

## 附:核查信源说明

核查中拦截到至少一处"生成式摘要与官方文档矛盾"的案例(A1 的 2025 跨平台烧录说法在 AI 摘要里流传,官方文档与维护者发言均否认)——**本项目的调研结论一律以可点开的一手来源为准**。主要信源:frcture.readthedocs.io、github.com(Boomaa23/open-ds、first-rust-competition/ds-rs、Redrield/Conductor、wpilibsuite/*、FRC-Utilities/*、mjansen4857/pathplanner、SleipnirGroup/Choreo、Team254/cheesy-arena-lite、Mechanical-Advantage/AdvantageScope)、docs.wpilib.org(stable/2024/2025/2026 版本对比)、frc-radio.vivid-hosting.net、wpilib.org/blog/the-2027-first-driver-station、developer.apple.com TN3179、chiefdelphi.com(维护者发言)。完整逐条证据(含 URL)存于本次核查工作流日志。
