# 2026.8.21——ErrorBoundary 收尾（PR #40）与 AGENTS.md 同步

## 今日完成

- **ErrorBoundary 合入**：上一会话因沙盒 CWD 故障（getcwd() EPERM）卡在 git 操作的 `feat/error-boundary`（main.tsx 包裹 / ErrorBoundary.tsx 新建 / styles.css 错误页样式），本会话 git 恢复后验证 typecheck 通过，提交 `3a821e1` 并以 **PR #40 合入**。远程功能分支已删除。
- **AGENTS.md**：按用户指令新建，内容与 CLAUDE.md 完全一致（已列入 .gitignore，不入库）。后续需保持两文件同步。

## 环境备注

- 上一 turn 的沙盒 CWD 问题在新 turn 中自愈，无需重启会话——印证"turn 切换后路径生效"的判断。
- **仓库分支结构变更**：合并 PR #40 时发现远程 `main` 已不存在，默认分支改为 `stable`（另有 `beta` 分支与若干新功能分支）。PR #40 实际合入 `stable`（merge commit `7287e4e`）。本地 `main` 仍指向旧提交，待用户确认新分支策略后处理。CLAUDE.md 中"合入 main"等表述需相应更新（本次未动，等确认）。

## 遗留问题

- 本地 `main` 分支的处置（改跟踪 `stable` 或删除重建）待用户决定。
- CLAUDE.md / AGENTS.md 中涉及 main 的约定是否改写为 stable，待用户确认。

---

# 2026.8.21（续）——分支纪律改为 beta→stable 两段式（用户指令）

## 今日完成

- **CLAUDE.md / AGENTS.md 同步更新分支纪律**：功能分支完成 → PR 合入 `beta` → 在 beta 上验证功能完善度与易用性（问题就地修复提交）→ 验证全部通过后再 PR 将 `beta` 合入 `stable`。两文件保持逐字节一致（cp + diff 校验）。
- 本地过时 `main` 分支已删除（其提交 9fc8246 已包含于 stable，无损失）。

## 环境备注

- 本时段 GitHub 网络多次闪断（443 连接超时 / HTTP2 framing layer 错误），间歇可用——push/PR 操作需重试。另发现远程 `beta`（9fc8246）落后 `stable`（de2dc8a），已尝试快进同步，受网络影响待确认。

## 遗留问题

- 远程 `beta` 是否成功快进到 `stable` 待网络恢复后确认（`git push origin stable:beta`）。
- 远程默认分支已为 `stable`，但本仓库 origin/HEAD 仍指向已删除的 origin/main——fetch 恢复后 `git remote set-head origin -a` 修正。

