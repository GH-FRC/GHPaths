# 2026.8.19（续）—— 可视化与遥测子系统设计（报告 v1.3 / ADR-0006）

> 另一会话（Cowork/Claude）的工作记录。**注意**：`feat/show-log` 分支上有本日早些时候的另一篇同名日志（演出日志与回放），两条分支合并时把两篇内容按时间顺序拼接即可。

## 今日完成

- **需求澄清**（与用户逐项确认）：可视化走双轨制——自研 2D/3D 与遥测图表为主，AdvantageScope 作备胎（自研有 bug 时顶上）；3D 要求"位置 + 机构细节都要"（含炮塔姿态）；遥测需全程录制 + 回放。
- **调研**（一手来源核实）：AS 的 NT4 客户端可连任意地址（含本地聚合服务器→一窗看 6 机）；WPILOG 为 AS 原生格式且支持多文件自动时间对齐；Custom Assets 机制 = .glb 组件 ↔ Pose3d[] 一一对应驱动机构动画（与自研 3D 视图可共用同一约定与同一份模型资产）；npm 有 wpilog-parser（读），写入器需自研（格式公开）。
- **报告 v1.3**：新增第八节"可视化与遥测子系统"（遥测约定 / 录制双格式 / 自研视图 / AS 三条备胎路径 / 优先级），需求表 +1 行、摘要 +1 条、风险清单 +2 行、路线图 Phase 2/3 增补、后续章节重排（原八~十一 → 九~十二），5.4 的"第九节"交叉引用同步改为"第十节"。
- **ADR-0006**（新）：自研为主 + AS 备胎的五条决策；README 版本引用与"第十节→第十一节"同步；architecture.md 模块表下加"规划中"说明。

## 决策与依据

- `componentPoses` 采用 AS 的 Pose3d 组件约定：备胎不是另做一套，而是同一协议的第二个消费者（模型资产也共用）。
- WPILOG 而非自造格式：与 AS 无缝互通是备胎策略的核心；JSONL 仍是 GHPaths 原生（已落地不动），浏览器阶段先做转换导出。

## 环境事故与修复

- 会话开始时仓库有陈旧 `.git/index.lock`（fetch 后无法 unlink，沙盒对挂载目录默认禁删）；`git branch` 测试又留下 `__wtest` 引用与锁。经用户授权开启 GHPaths 目录删除权限后已全部清理，git 写操作恢复正常。**沙盒会话在此仓库做 git 写操作前建议先确认删除权限已开启。**

## 遗留问题

- 沙盒无 gh CLI 与 GitHub 凭据：docs/visualization-telemetry 分支提交后**推送与 PR 创建/合并需在用户侧完成**（compare 链接见会话总结）。
- WPILOG 写入器、聚合转发服务器、componentPoses topic 具体字段——进入实现阶段时在 show-protocol 落地。

---

# 2026.8.19（续二）—— macOS 分发与安装体验研究（报告 v1.4 §7.5 / ADR-0007）

> 同会话（Cowork/Claude）续写。

## 今日完成

- **一手核查三个参照工具**：AdvantageScope 的 CI（build.yml）有 Developer ID 证书 + 公证凭据加载步骤（真·双击即用）；PathPlanner 的 CI（pathplanner-ci.yaml）无任何签名步骤；Choreo 维护者在 issue #428 自述"让 Tauri 跳过签名"——**三个参照里只有 AS 达标**，"PP/Choreo 也能干净安装"实为点击警告绕过或旧版 macOS。
- **Gatekeeper 规则确认**：macOS 15 Sequoia 起移除"右键→打开"绕过，未公证 app 须进设置点"仍要打开"；唯一正规路径 = Apple Developer Program（$99/年）+ Developer ID 签名 + notarytool 公证，Tauri v2 原生支持（参照 AS 做法）。
- **权限弹窗真相**："本地网络"弹窗（macOS 15+）与签名公证无关、无法消除，但为应用内对话框点"允许"即完成（非设置项；AS 也弹）；防火墙弹窗默认不出现（防火墙默认关、签名 app 自动放行）。
- **报告 v1.4**：新增 §7.5（参照实况表 + Gatekeeper 规则 + 权限弹窗真相 + 打包要求）；§7.1 矩阵 +1 行、需求表 +1 行、风险清单 +1 行、路线图发版里程碑注记、结论 +1 条、参考文献 +6 条。
- **ADR-0007**：签名+公证决策、sidecar 打包、Info.plist 键（NSLocalNetworkUsageDescription 等）、打包后实测两项（公证 dmg 零警告；子进程本地网络权限按责任进程归属父 app）。

## 流程事故与改进（多会话并行）

- 本会话调研期间（08:52~08:53），另一会话将主工作区从 docs/mac-distribution 切回 feat/show-log 并连提两个 commit——共享工作区内容错乱，本会话一度基于错误分支内容操作（幸未误提交，9dfe5e2 提交经核验完整无损）。
- **改进**：本次改用 `git worktree`（/tmp 检出 docs/mac-distribution）完成提交，全程不触碰主工作区。**多 AI 会话并行操作同一仓库时，辅助类会话（docs/research）应一律用 worktree 隔离，不与主开发会话争用 checkout。**

## 遗留问题

- Apple Developer Program 采购（$99/年）待用户决策；采购完成前对他人装机走临时 SOP（设置→隐私与安全性→仍要打开）。
- docs/visualization-telemetry 与 docs/mac-distribution 两分支仍待推送/PR（沙盒无凭据，用户侧执行）。

第二天：补齐 Phase 2 MVP 的最后一块——演出日志与回放（报告 Phase 2 定义：6 机 NT 聚合、实时画布、一键启停、**基本日志**）。

# 2026.8.19 —— 演出日志与回放上线（feat/show-log，第三会话）

## 今日完成

- **录制**（`useRecorder` + show-protocol `ShowLogRecord` v1）：JSONL 一行一条——meta/演出事件（start/hold/resume/stop/ended）/位姿（带 tShowUs 演出时钟）/健康（按变化）/DS 状态（按内容去重，10Hz 广播不刷屏）。开演自动开始、结束演出停止，内存缓冲上限 30 万行，导出 .jsonl 下载。
- **回放**（`useReplay`）：文件加载 → 时间轴归一化（tRecMs 是 performance.now 绝对值，解析时平移归零）→ 拖动/播放（rAF）双时钟显示（录制时长 + 演出时钟）→ 画布按光标渲染位姿（二分查找）与 5s 轨迹窗 → 演出事件标记。
- **接线**：useRobots 加数据流 tap（onPose/onHealth，ref 轮换保稳定）；useShow 加 onEvent；multi-DS 状态 effect 落盘。
- **端到端实测**（浏览器）：录制 19s 演出 → 导出 2468 行 → 回放拖到 50% → 时间轴 9.5s、演出时钟 9.5s、机器人 x=-2.88 与理论进度 -4.2+0.14×9.5=-2.87 精确吻合。日志还如实记录了两场"异常演出"（就位检查拒绝的第二轮开演、multi-DS 迟启动被丢的 enableAll）——**日志的可信度本身就是验证对象**。

## 决策与依据

- 位姿行带 tShowUs：回放时间轴（录制钟）与编排分析（演出钟）解耦，双钟并显。
- 浏览器无文件写入：内存缓冲 + 手动导出（后续 Tauri 打包时可换本地落盘，格式不变）。
- 调试再次确认三件事按设计工作：enableAll 在 multi-DS 未连时被丢弃（反延迟自启动）；就位检查拒绝非起点开演；后台标签节流致 held。排障反射依旧是"先怀疑安全链"。

## 下一步

- 对抗性审查（本特性）→ PR 合入。
- Phase 2 收尾候选：演出统计（间距曲线/超界告警回放叠加）、时钟发布 Worker 化、robot/ 真实 WPILib 工程。
- 硬件盘点（用户侧，不变）。

## 遗留问题

- 承接 8.18：仓库无 LICENSE；协议待实测 3 项；舞台/底盘占位尺寸。
- 回放轨迹窗固定 5s/120 点；长演出统计（平均间距等）未做。

---

# 2026.8.19（续三）—— 分发决策定稿：完全免费方案（报告 v1.5 / ADR-0007 v2）

> 同会话（Cowork/Claude）。用户四点定稿：完全免费（一分不付）；不走学校认证（教育减免取消）；主渠道 = 浏览器下载安装包直接安装；终端/Homebrew 保留并行，releases 页含安装包与教程。

## 今日完成

- 向用户讲清免费分档（教育减免 / 终端·Homebrew / 拷贝 / 浏览器下载）与免费物理上限（每台 Mac 首装一次"仍要打开"+ 密码，无法再少），用户定案免费方案。
- **报告 v1.5**：§7.5 结论改为免费方案六条（主渠道 / 升级免再批准 / 并行渠道 / Releases 页规范 / 工程红线 / 未来可选升级），摘要、需求表、§7.1 矩阵、风险清单、路线图、结论、页脚同步。
- **ADR-0007 修订为 v2**（本 ADR 未合入 main，就地修订并在文末保留 v1→v2 修订历史）：七条决策 + 实测清单四项；releases 模板（安装包 + 浏览器/终端/Homebrew 三渠道教程）列入发版 checklist。

## 关键认知

- 免费方案的"每版本烦一次"靠**应用内更新**压成"每机终身一次"——Tauri updater 下载不经浏览器 quarantine，是免费方案里最值钱的一条。
- ad-hoc 签名必须保留：它决定用户看到的是"仍要打开"（可图文引导）还是"已损坏"（只剩 xattr 终端路）；构建配置不得剥离。

## 遗留问题

- 待打包阶段实测：updater 升级零弹窗、sidecar 权限归属、Homebrew tap、"仍要打开"流程可达性（共四项，见 ADR-0007）。
- docs/mac-distribution 分支待推送/PR（用户侧）；releases 模板在首次发版前落地。

# 2026.8.19 —— 机器人端工程骨架 + 时钟节拍 Worker 化（feat/clock-worker→#9、feat/robot-project）

## 今日完成（本会话续）

- **时钟/心跳节拍 Worker 化**（PR #9）：tick-source 单例 Worker（100ms/拍，失败回退 setInterval）；useShow 时钟广播与 useMultiDs uiPing 均改走 Worker 节拍。实测边界如实入册：Worker 消除"可见但节流"，但完全隐藏标签页 Chrome 冻结页面 JS（45s 缺口实测）→ 跳变防护正确闩锁；根治=Tauri 原生打包（ADR-0007）。副作用确认：双控制台时钟冲突被防护正确拒绝——README 增"单主控时钟"约束。
- **报告 v1.5 合入**（PR #10）：分发决策定稿免费方案（ADR-0007 v2）；用户指令入册——**一切修改以计划书最终版为准**（CLAUDE.md + 记忆）。
- **机器人端工程骨架**（feat/robot-project）：WPILib 2026 GradleRIO 工程（未经编译验证，build.gradle 尾部有语法自检清单）；ShowClock（断时钟即停/跳变防护/样本间本地推进——与 fake-robot 同语义）；ShowCommandReceiver（arm/start/stop/hold/resume + 未 arm 拒 start）；ShowCoordinator（运动许可链五环与 sim 一致 + 地理围栏越界即停）；TelemetryPublisher（pose double[] + health JSON）。Phase 0 到手即部署。

## 决策与依据

- 机器人侧时钟跳变防护与 sim 端用同一包络公式（到样间隔+150ms、不回跳）——sim 八关探针验证过的语义直接移植，Phase 0 实测两边行为一致即闭环。
- health 用 StringTopic 而非 NT4 "json" 类型串：nt-link 按 topic 名匹配+内容解析，类型串差异不影响互通（类型码同为 4）。
- 就位检查在 ShowCoordinator（需要位姿与路径起点），与 fake-robot 的位置一致。

## 遗留

- Java 工程未经编译（无 GradleRIO 下载条件）——首次部署必须过 WPILib VS Code Build。
- gradle wrapper 未生成（无 gradle 命令）；Phase 0 用 WPILib VS Code 的 gradlew。

## 会话续：机器人工程对抗性审查修正（feat/robot-project 续）

两代理审查（语义对照 sim 基线 + WPILib 2026 API/GradleRIO 联网核实）12 项发现全修：

- **必修（编译级,5 项）**：build.gradle 三处致命配置（`artifacts.deployedJava` 越权引用、`implementation` 在 dependencies 块外、不存在的方法）→ 整体重写为 2026 官方模板形态（含 fat jar 与静态部署块）;vendordep 缺 frcYear/uuid 错 → 换官方 2026.1.2 版;`subscribe("{}", 0.02)` 重载不存在 → `PubSubOption.periodic(0.02)`;`readQueue()` 当 Iterator 用 → 增强for 遍历数组。
- **必修（语义,1 项）**：health 格式串 estopped 硬编码 false 且参数错位（上一轮 Python 补丁因 Java 转义引号未匹配而静默失败——教训：跨语言字符串补丁必须验证落盘）→ 重写格式串+转义函数。
- **should（4 项）**：跳变防护补 stop→onShowStop() 清锚点（主控 stop 归零时钟不误报）;health 字段来源改为 isDSAttached/isEnabled/isEStopped（与 sim 逐字段对齐,dsLinked 不再混入使能判定）;start 接受域放宽为"已 arm 且未 stop"（hold 后重复 start = resume,不再锁死）;时钟锚点改本地到样时刻（与主控 Cristian 估计解耦）。
- **nit（2 项）**：pose[0] tShowUs 仅运动时推进（回放取证时间轴不失真）;DriveInterface 增 stop()（许可断开对执行器显式停,防御"保持最后指令"型驱动）。

复验：npm 侧 typecheck 0 错;Java 无本地编译条件（macOS javac 无运行时）——首次编译验证维持"WPILib VS Code Build"清单项。

# 2026.8.19（续四）—— Tauri 桌面壳上线（feat/tauri-shell，ADR-0007 v2 首个里程碑）

## 今日完成

- **环境**：rustup 安装（官方源被卡 8 分钟，换 rsproxy.cn 镜像秒装；~/.cargo/config.toml 配 crates 镜像）。
- **Tauri v2 壳**（apps/console/src-tauri/）：标准模板（Cargo/build.rs/lib.rs/capabilities）+ tauri.conf.json（dmg+app 目标、窗口 1280×860）+ **Info.plist 合并文件**（NSLocalNetworkUsageDescription，v2 的 infoPlist 收路径不收对象——源码级核实）+ 程序化生成图标（六机器人点阵，Python 手写 PNG）。
- **实测产物**：GHPaths_0.1.0_aarch64.dmg（2.5MB）+ GHPaths.app；ad-hoc 签名在（红线确认）；声明已合并；无隔离属性；**实机启动**：窗口常驻、连上 sim NT4（12 连接）与 multi-DS。
- **打包实测抓到真 bug**：multi-DS 的 Origin 白名单（安全审查轮加的）只放行 http/https，打包壳页面 Origin 是 `tauri://localhost` → 5899 被拒。修复：白名单加 tauri: 协议。**教训：每个安全收紧都要想清楚合法客户端的全集，打包形态改变了 Origin——好在实测逮住了。**

## 决策与依据

- 首个里程碑范围 = 壳 + DMG + 签名 + 本地网络声明；**sidecar（sim/multi-DS 子进程收进 .app）与 updater 属下一里程碑**（ADR-0007 实测清单的四项也归那时）。
- 当前打包 app 仍需终端跑 `npm run sim` + `npm run ds`——开发回路不变，但排练已有原生窗口（无标签节流）。

## 遗留

- sidecar/updater/发版模板（ADR-0007 清单）；图标为程序化占位（可换正式设计）。

# 2026.8.19（续五）—— sidecar 集成：双击全搞定（feat/tauri-sidecar）

## 今日完成

- **sidecar 架构落地**：esbuild 单体 CJS bundle（sim 206KB / multi-DS 137KB，含 workspace 源码依赖）+ Node 运行时（65MB）经 Tauri externalBin 收进 .app——不依赖用户机器装 Node，bundle 签名整体覆盖（ADR-0007 决策 6）。DMG 2.5MB → 19MB。
- **进程管理**：Rust 侧 start/stop/status 命令（前端面板 + 首启自启）；日志重定向 /tmp/ghpaths/。
- **端到端实测**：双击 .app → 两个后端自启 → NT 15801/DS 15101/控制 5899 全就绪 → WebView 连通 → enableAll 传导 → 演出链完整。退出 → 零孤儿零僵尸。
- **实测抓了三个真 bug**：①bundle 无扩展名在 repo 内被 package.json type:module 当 ESM 解析（require 报错）→ externalBin 带 .cjs 后缀；②窗口 Destroyed 事件在 AppleScript/系统退出路径不触发 → 曾留孤儿 → 改 RunEvent::Exit 保证钩子；③手快双击启停报错 → 前后端全幂等化。
- **聚焦审查（1 代理,8 项全修）**：SIGKILL 绕过 multi-DS 优雅关停 → TERM→1.5s 有界等待→KILL→wait 收尸（TERM 路径实测「全部 disable 后退出」）；僵尸进程 → wait() 补齐；首启吞错 → 真实失败展示；单锁竞态、tauri Mutex 不可用→中毒恢复、死事件、非 darwin triple 守卫。

## 遗留

- updater 与发版模板（ADR-0007 剩余实测项）；图标可换正式设计。
- sidecar 的 sim 模式固定 6 台/9001 起（配置界面 Phase 2 余项）。

# 2026.8.19（续六）—— WPILOG 导出落地（feat/wpilog-export，ADR-0006 决策 3）

## 今日完成

- **WPILOG 写入器**（apps/console/src/wpilog.ts）：逐字节对照 allwpilib v2026.2.1 的 DataLog.cpp/Reader 源码实现——文件头（'WPILOG'+版本 0x0100+extraHeader）、变长记录头（bitfield 长度编码）、控制记录（kControlStart=0，u32 前缀字符串）、schema 记录（'/.schema/<类型>'，类型串 'structschema'）。
- **JSONL→WPILOG 转换器**（wpilog-export.ts）：每机 Pose2d 原生结构（AS 2D 场地直接渲染）+ 4 元位姿数组 + 演出时钟 + DS/健康布尔变化 + 电池 + 事件——AdvantageScope 双击即看。
- **往返验证**（wpilog-roundtrip.ts）：用 wpilog-parser（社区库,ADR-0006 调研确认）读回——目录/数值/时间序列逐项断言通过;结构记录从原始字节层校验（3 doubles 精确）。
- **UI**：导出按钮旁加「导出 WPILOG」。

## 决策与依据

- 线格式三个易错点全踩过并修正：kControlStart 是 0 不是 1（枚举首值）;schema entry 类型串是 'structschema' 不是 'struct'（WPILib StructBufferUtil 惯例,AdvantageScope/wpilog-parser 按此识别）;嵌套结构 schema 必须全部注册（Pose2d→Translation2d+Rotation2d）。
- wpilog-parser 2.2.0 的 structschema 解码有 name.slice(16) bug（'/.schema/' 实长 9）——其 decodeRecords 会丢结构记录;验证走原始字节层绕开。AdvantageScope 按 WPILib 原生约定读（我们逐字节对照的就是 WPILib 本体实现）。

## 遗留

- AdvantageScope 实机打开验证（用户侧,装 AS 即可双击 .wpilog 文件）;聚合转发服务器与 componentPoses 实时 topic 属后续。

# 2026.8.19（续七）—— 遥测图表：间距曲线 + 电池 sparkline（feat/telemetry-charts，ADR-0006）

## 今日完成

- **最小机间距实时曲线**（SpacingChart）：单序列 emphasis 形态（dataviz 规程——6 色机器人板经验证器确认相邻 CVD 分离不足,单线+阈值线回避分类板;单线无图例,标题即名）;2px 线宽、隐性网格、安全阈值虚线（状态色+直接标注）、末端直接标注、悬停十字线+浮签（全图命中）。60s 滑窗,20Hz。
- **电池 sparkline**（卡片内,Sparkline）：单序列单色（机器人色=卡片上下文身份）;1Hz 采样 10 分钟滑窗。
- **useTelemetry**：始终开启的轻量环缓冲（与录制的落盘通道独立）;间距全对 O(n²) 计算(6 机 15 对,可忽略)。
- 实测：演出中曲线 y 值与理论精确吻合（恒定 1.1m 车道距 → 持平线于阈值上方）;阈值线/末端标注/浮签 DOM 验证;inspect 确认 2px 线宽渲染。

## 决策与依据

- 图表插在画布与卡片面板之间、回放时隐藏（回放画布被接管时不显示无关实时数据）。
- 阈值 1.05m 取自 field-model createDemoShow 的构建期碰撞断言值（同一安全标准两处引用,Phase 2 余项：提炼为共享常量）。

## 遗留

- 间距阈值常量与 field-model 共享;电池低电压状态色告警（<11V serious）;演出时钟漂移曲线。

# 2026.8.19（续八）—— Phase 3 开坑：多机路径编辑器第一块（feat/path-editor）

## 今日完成

- **编辑画布**（EditorCanvas）：多图层渲染（选中层高亮/其余淡化）;航点交互（选中白环、拖动移动、段中点插入预览、Delete 删除中间点/端点固定）;演出运行中编辑暂停横幅。
- **编辑器状态**（usePathEditor）：选中机器人/航点管理;moveWaypoint/insertWaypoint/deleteWaypoint 直接操作 field-model 的 Show 数据;**实时碰撞检测**（全对 minSeparationM,违规路径标红 + 编辑栏告警条）;JSON 导出/加载（Show 格式 = field-model 单一来源）。
- **UI 接线**：「路径编辑」模式切换（header 按钮,演出中禁用）;编辑工具栏（载入/导出 JSON、恢复演示、碰撞状态）;机器人标签页（图层选择）。
- 浏览器实测：6 机标签、23 可拖航点 + 2 固定端点 + 125 背景航点全渲染;选中环工作;「无碰撞风险 ✓」正确显示;合成事件无法完整模拟 pointer capture 拖动（浏览器原生手势,数据链路已验证——selection → moveWaypoint → minSeparationM → violation 渲染为同一 useMemo 链）。

## 决策与依据

- 时间模型简化：插入航点取相邻时间中点（线性内插）;手动时间轴编辑属 Phase 3 后续。
- 拖动的 y 轴钳制到地理围栏内（编辑也不能画出安全边界）。
- 碰撞检测阈值 1.05m 与 field-model createDemoShow 构建期断言同源。

## 遗留

- 拖动 UI 的端到端手势验证（需真人操作或 Playwright 级浏览器驱动——合成事件无法完整模拟 pointer capture）;时间轴编辑;轨迹平滑（PathPlanner）;3D 视图（ADR-0006 优先级二）。

# 2026.8.19（续九）—— 3D 全场监控视图（feat/3d-view，ADR-0006 优先级二）

## 今日完成

- **Stage3D**（react-three-fiber + three.js）：3D 全场实时监控——舞台地板 + 半透明边界墙 + 1m 参考网格;机器人 = 盒子 + 车头方向楔形标记（几何占位,ADR-0006：低模起步,Phase 3 换 .glb 模型管线）;轨迹尾巴线。
- **坐标系映射**：field-model 世界（+x 右/+y 向观众）→ three.js（x→x、y→z）;heading 绕 three y 轴取负号。
- **2D/3D 切换**：header 一键切换;编辑模式与回放模式互斥。
- **版本兼容踩坑**：R3F 9.x 需 React 19（本仓 React 18 → `Cannot read properties of undefined (reading 'S')`）;降级 @react-three/fiber@8 解决。vite 依赖缓存（.vite/deps）在降级后需清除才生效。

## 遗留

- OrbitControls（相机拖拽/缩放——需 @react-three/drei）;机构姿态 componentPoses（Phase 3）;.glb 模型资产管线。

# 2026.8.19（续十）—— 编辑器数据统一：编辑 = 演出预览 = 录制元数据（feat/editor-drives-show）

## 今日完成

- **数据统一**：编辑器的 Show 数据（editor.show）成为全部下游的单一来源——2D 路径预览、演出时长（useShow 的 durationShowUs）、录制元数据（recorder.begin 的 showId）、编排路径渲染。编辑器改路径 → 预览即刻变 → 演出时长自动调整。
- **净化**：App.tsx 删掉独立的 demoShow 声明与 DEMO_SHOW_ID 引用;编辑器内部仍然用 createDemoShow 初始化（`恢复演示`按钮同源）。
- 诚实边界：机器人端（fake-robot/robot）的路径加载仍然是内置演示——真正的路径分发到各机（arm 命令带路径数据或文件部署）属 Phase 2 余项「路径部署」;UI 层已统一。

## 遗留

- 机器人端路径分发（arm → 本机轨迹,替换 demoPath 硬编码）;时间轴编辑;轨迹平滑。

# 2026.8.19（续十一）—— 机器人端路径分发：编辑器路径驱动机器人（feat/robot-path-dispatch）

## 今日完成

- **协议扩展**（show-protocol）：ShowCommand.arm 增加 `path?: unknown` 字段——编辑器编辑的本机路径（RobotShowPath JSON）随 arm 命令下发,机器人直接装载。这是「路径部署」的轻量版（无需文件分发）;真机同样收此字段。
- **console → 机器人**：useShow.start 的 arm 命令带 `paths.get(team)`（编辑器 Show 的本机路径 map）;UI 侧编辑 = 演出 = 机器人路径,全链单一数据源。
- **fake-robot**：handleCommand 的 arm 分支接受 path 字段,结构校验（isValidRobotPath——字段名/数值类型/≥2 航点）后装载;无效/缺失退回内置演示。**机器人现在开演时走编辑器画的路径。**
- **实测闭环**：开演 → 6 台机器人 y 分布 -2.54..2.96 = 编辑器六车道（-2.75..2.75 + 波形偏移）——路径分发生效;演出时钟 25.8s 运行正常。

## 遗留

- 路径体积：复杂路径的 JSON 体积（NT4 命令 topic 的单条消息上限——复杂编排可能需要文件分发通道）。
- 真机（robot/ghpaths-robot）的 ShowCommandReceiver 同样接受 path（Java 侧解析 RobotShowPath——TODO Phase 0 编译验证后实现）。

# 2026.8.19（续十二）—— 3D OrbitControls（feat/orbit-controls）

## 今日完成

- **OrbitControls**（three.js 原生 `examples/jsm/controls/OrbitControls.js`,不用 drei）：拖拽旋转 / 滚轮缩放 / 右键平移;极角限制 ≤ 90°（不穿地下）;距离钳制 5~30m;阻尼。R3F 自定义 hook 桥接（useThree + useFrame update）。
- **版本兼容**：drei 10 需 R3F 9（React 19）、drei 9 有 workspace hoisting 解析问题、drei 7 依赖旧 three internals（stats.min 不存在）——全部不适配,改用 three 原生实现（零额外依赖,更稳）。

实测：3D canvas `touch-action:none`（OrbitControls 接管指针）——可交互验证。

# 2026.8.19（续十三）—— 回放统计叠加（feat/replay-stats）

## 今日完成

- **ReplayStats**：回放加载时计算全程统计——最小机间距（+发生时刻）、间距超阈值时段数、最低电池。全对采样（100ms 间隔,与实时间距曲线同算法）。
- **UI**：回放时间轴上叠加统计条——最窄间距（绿/红色标注是否超阈值）、发生时刻、超阈值段数。

## 遗留

- 间距超阈值的具体时段列表（时间轴上红色标记条）;电池统计完善（parseLog 未收 battery 行——当前 minBatteryVolts 恒为 ∞→0）。

# 2026.8.19（续十四）—— 安全间距常量共享（refactor/safety-const）

## 今日完成

- field-model 导出 `SAFETY_MIN_SEPARATION_M = 1.05`;三处消费方（createDemoShow 断言、usePathEditor 碰撞预警、SpacingChart 阈值线）全部改用共享常量——日志遗留项清偿。

# 2026.8.19（续十五）—— 机器人端 Java 路径分发（feat/robot-java-path）

## 今日完成

- **ShowCommandReceiver.arm 接受 path 字段**：极简 JSON 路径解析（waypoints 数组的 xM/yM/headingRad/tShowUs 提取;坏数据整体拒绝）;`RobotPath`/`PathWaypoint` 内部类型（Phase 2 接 PathPlannerLib 后换正式类型）;`armedPath()` getter 供 ShowCoordinator 消费。未经编译验证（macOS javac 无运行时——首次编译 = WPILib VS Code Build,清单在 build.gradle 尾部）。

---

# 2026.8.19（续六）—— 两个新功能入册：三语言设置 + 场地地图（报告 v1.6 / ADR-0008）

> 同会话（Cowork/Claude）。用户需求：① Preferences 三语言（简/繁/英）；② 赛季场地内置 + 用户导入（含 AprilTag）。

## 今日完成

- **核实**：AS 场地资产机制（2D 图 + 角点像素标定 + 尺寸米 + 可选 glb）；AdvantageScopeAssets 仓库 **BSD-3**（赛季场地素材的合法参照来源）；AprilTag 布局有 WPILib 官方 JSON 格式（PhotonVision 同格式）。演出环境不保证联网 → 内置而非 AS 式联网下载。
- **报告 v1.6**：§8.7 语言设置（react-i18next；繁体 OpenCC 转换 + 术语表校对；机制骨架 Phase 2、全量覆盖等 Phase 3 文案冻结）；§8.8 场地地图（赛季内置 + 用户导入场地包含 AprilTag 兼容 WPILib 格式；field-model 新增 FieldMap；AprilTag 单一事实来源——渲染与导出给 PhotonVision 共用）；需求表 +2 行、摘要 +1 条、路线图/参考文献/页脚同步。
- **ADR-0008**：六条决策 + 后果（三语言同步成本、素材版权复查项、新待办清单）。
- 流程：自 main（#23）开 docs/i18n-fieldmaps，worktree 提交，不碰主工作区。

## 遗留

- 实现阶段待办：Preferences 骨架、i18n 接入、FieldMap 类型与导入 UI、赛季场地打包脚本；精确 JSON schema 对齐 AS 文档与资产仓库实物。

# 2026.8.19（续十八）—— 演出时钟漂移曲线（feat/clock-drift-chart）

## 今日完成

- **useTelemetry.clockDrift**：每秒采样演出钟与墙钟的偏差率（ppm = (showΔ-wallΔ)/wallΔ × 1e6）;60s 环缓冲。数据源：pose 的 tShowUs（演出钟）对照 performance.now（墙钟）——漂移率即演出时钟质量的可观测指标。
- **UI**：间距曲线旁的迷你 drift 曲线（复用 SpacingChart,阈值 0 = 无漂移基线）。
- 日志遗留项清偿。

## 遗留

- drift 图表标题区分（当前两图都显示"最小机间距"——复用组件的副作用,待拆分或参数化标题）。

（续十八补）SpacingChart 标题参数化（title + unit props）——两图不再同名。

（续二十补）回放电池统计修复——parseLog 收 DS 行的 batteryVolts（日志遗留项清偿）。
